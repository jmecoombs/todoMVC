var listItems = function (text) {
 var result = [];
    for(var index = 0; index < text.length; index++) {

        var = newData =
        var completed = '';
        var checked = '';
        
    }
    document.write('<li>' + text[i] + '</li>');
    return result;
}